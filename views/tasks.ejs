<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>All Tasks</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>To-Do List</h1>
        <ul>
            <% tasks.forEach(task => { %>
                <li><a href="/task?id=<%= task.id %>"><%= task.title %></a></li>
            <% }) %>
        </ul>
        <a href="/add-task">➕ Add New Task</a>
    </div>
</body>
</html>
